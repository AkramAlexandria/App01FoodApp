<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app01foodapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainCart"></activity>
        <activity android:name=".MainFood" />
        <activity android:name=".MainSignIn" />
        <activity android:name=".MainSignUp" />
        <activity android:name=".MainLogin" />
        <activity android:name=".MainSlider04" />
        <activity android:name=".MainSlider03" />
        <activity android:name=".MainSlider02" />
        <activity android:name=".MainSlider01" />
        <activity android:name=".MainDescription" />
        <activity android:name=".MainSelection" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>